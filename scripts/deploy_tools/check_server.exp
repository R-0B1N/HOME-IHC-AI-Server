#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no admin123@home-ihc-ai-server "ls -la ~/opt/crm"
expect {
    "*?assword:*" {
        send "P@ssw0rd\r"
        exp_continue
    }
    eof
}
