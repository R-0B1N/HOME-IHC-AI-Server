#!/usr/bin/expect -f
set timeout -1
spawn ssh -t -o StrictHostKeyChecking=no admin123@homeihc-ai-server "sudo bash -c 'docker ps -a | grep n8n'"
expect {
    "*?assword*" {
        send "P@ssw0rd\r"
        exp_continue
    }
    eof
}
