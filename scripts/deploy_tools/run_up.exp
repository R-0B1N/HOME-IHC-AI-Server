#!/usr/bin/expect -f
set timeout -1
spawn ssh -t -o StrictHostKeyChecking=no admin123@homeihc-ai-server "cd /opt/crm && sudo bash -c 'docker compose up -d --build property-dashboard api worker cloudflared'"
expect {
    "*?assword*" {
        send "P@ssw0rd\r"
        exp_continue
    }
    eof
}
