#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server "ls -la /opt/crm/dashboard && cat /opt/crm/dashboard/Dockerfile 2>/dev/null || echo 'No Dockerfile'"
expect "*?assword:*"
send "P@ssw0rd\r"
expect eof
