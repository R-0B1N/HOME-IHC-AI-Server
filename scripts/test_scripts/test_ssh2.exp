#!/usr/bin/expect -f
set timeout -1
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server "cd /opt/crm && docker compose build api worker && docker compose up -d api worker"
expect {
    "*?assword:*" {
        send "P@ssw0rd\r"
        exp_continue
    }
    eof
}
