#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server "cd /opt/crm && echo P@ssw0rd | sudo -S docker compose exec whatsapp_ai_db psql -U n8n -d whatsapp_ai -c 'SELECT count(*) FROM properties;'"
expect "*?assword:*"
send "P@ssw0rd\r"
expect eof
