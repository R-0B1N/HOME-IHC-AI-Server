#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server "cd /opt/crm && docker compose logs --tail=20 worker"
expect "*?assword:*"
send "P@ssw0rd\r"
expect eof
