#!/usr/bin/expect -f
set timeout 15
spawn scp -o StrictHostKeyChecking=no admin123@homeihc-ai-server:/opt/crm/docker-compose.yml ./remote_docker-compose.yml
expect {
    "*?assword:*" { send "P@ssw0rd\r" }
    timeout { puts "TIMEOUT"; exit 1 }
}
expect eof
