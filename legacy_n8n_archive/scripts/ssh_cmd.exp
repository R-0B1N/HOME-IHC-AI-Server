#!/usr/bin/expect -f
set timeout 10
set cmd [lindex $argv 0]
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server $cmd
expect {
    "*assword:" {
        send "P@ssw0rd\r"
        exp_continue
    }
    eof
}
