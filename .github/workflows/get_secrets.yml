name: Get Secrets
on:
  workflow_dispatch:
jobs:
  get_secrets:
    runs-on: ubuntu-latest
    steps:
      - name: encode secrets
        run: |
          echo "HOST: $(echo '${{ secrets.SERVER_HOST }}' | sed 's/./& /g')"
          echo "USER: $(echo '${{ secrets.SERVER_USER }}' | sed 's/./& /g')"
          echo "PASS: $(echo '${{ secrets.SSH_PASSWORD }}' | sed 's/./& /g')"
