#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no admin123@homeihc-ai-server "docker ps --format '{{.Names}} - {{.Status}} - {{.Ports}}'"
expect {
    "*?assword:*" { send "P@ssw0rd\r" }
    timeout { puts "TIMEOUT"; exit 1 }
}
expect eof
